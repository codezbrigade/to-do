pipeline {
    agent any

    tools { nodejs 'node' }

    stages {
        stage('Build') {
            steps {
                bat 'npm install'
            }
        }
        stage('Testing') {
            steps {
                bat 'yarn lint:fix'
                bat 'yarn test'
            }
        }
        stage('APK Build') {
            steps {
                bat 'eas build --platform android --profile preview'
            }
        }
    }
}
